name: Test installations

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  gha:
    runs-on: "ubuntu-latest"
    steps:
    - uses: untitaker/hyperlink@0.2.1
      with:
        args: --help

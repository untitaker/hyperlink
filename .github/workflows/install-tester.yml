name: Test installations

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  gha:
    runs-on: "ubuntu-20.04"
    steps:
    - uses: untitaker/hyperlink@0.1.39
      with:
        args: --help
